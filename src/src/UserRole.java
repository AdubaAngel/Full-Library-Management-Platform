public enum UserRole {
    PATRON,      // Regular library user
    EMPLOYEE,    // Library staff
    MANAGER,    // Full access
    GUEST,      // Temporary User, limited access
    VOLUNTEER,  // Can help but not full employee,
    JUNIOR_EMPLOYEE  // Entry-level staff, limited permissions
}
